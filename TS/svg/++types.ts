namespace Svg {
   export namespace Types {
      export type transformMatrix = {
         a: number,
         b: number,
         c: number,
         d: number,
         e: number,
         f: number
      };
   }
}